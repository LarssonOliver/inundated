code:
  name: code
  in: query
  required: true
  schema:
    type: string
state:
  name: state
  in: query
  required: true
  schema:
    type: string
XSRFTokenHeader:
  name: X-XSRF-TOKEN
  in: header
  required: true
  description: Anti-CSRF token extracted from the XSRF-TOKEN cookie.
  schema:
    type: string
