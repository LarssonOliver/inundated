# Shared schema fragments — not standalone schemas, just anchors for reuse.

HexColor:
  type: string
  pattern: "^#(?:[0-9a-fA-F]{3}){1,2}$"

TagIdList:
  type: array
  uniqueItems: true
  items:
    type: string
    format: uuid
